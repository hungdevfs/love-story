import React from "react"
import { View, Text, StyleSheet } from "react-native"

const Avatars = ({ username1, avatar1, username2, avatar2 }) => {
  return (
    <View>
      <Text>Avatars</Text>
    </View>
  )
}

export default Avatars

const styles = StyleSheet.create({})
